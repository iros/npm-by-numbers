.layout.web {
  padding: 0px $body_padding $body_padding $body_padding;

  .help:hover {
    background-color: $links_red;
    border-color: $links_red;
  }

  #footer {
    position: fixed;
    height: 25px;
    background-color: white;
    border-top: 1px solid $border_color_light;
    bottom: 0;
    left: 40px;
    right: 40px;
    text-align: center;
    font-size: 0.7em;
    color: #aaa;
    padding-top: 3px;
  }
}
